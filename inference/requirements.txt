# ── TFLite 推理（纯 CPU，无 GPU 依赖）─────────────────────────────────────
# 优先使用轻量包，回退逻辑在 runtime.py 里
# x86_64 / aarch64 均有预编译 wheel，安装后约 30 MB
ai-edge-litert>=1.0.0

# ── micro_speech 前端特征提取 ──────────────────────────────────────────────
# 与训练时完全一致：40 维 / 10ms，含噪声抑制 + PCAN 自动增益
pymicro-features>=2.0.0

# ── 音频处理 ───────────────────────────────────────────────────────────────
numpy>=1.23,<2.0
soundfile>=0.12       # 读写 wav/flac/ogg，无需 ffmpeg
resampy>=0.4          # 高质量重采样到 16kHz

# ── 实时麦克风 ────────────────────────────────────────────────────────────
# 需要先安装系统依赖：apt-get install -y portaudio19-dev
pyaudio>=0.2.14
